# Agent Instructions

Use `CLAUDE.md` as the compact project brief. Keep searches targeted, avoid generated/dependency/log/reference folders, and preserve unrelated user changes in the dirty working tree.

